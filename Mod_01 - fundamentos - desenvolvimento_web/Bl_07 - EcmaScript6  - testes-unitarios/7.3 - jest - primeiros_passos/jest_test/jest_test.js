/*
Obs.:
O código será desenvolvido neste arquivo .js e seus parâmetros seram testados no arquivo .test.js (ou .spec.js).

para que o arquivo possa ser acessado pelo teste é necessário exortá-lo:
module.exports = nome_da_funcao;
*/  

const sum = (a, b) => a + b;

//Exporta a função para ser testada no arquivo .test.js:
module.exports = sum;